---
description: Save session state to the plan file before /clear or ending work
---
Perform the handoff protocol for the current work:

1. Identify the active plan file (docs/plans/*.md; ask if ambiguous).
2. Tick checkboxes for completed units.
3. Rewrite the STATE section: (a) what is done, (b) exact next step,
   (c) non-obvious gotchas discovered this session.
4. Append any decisions made this session to the Decision log
   (decision, reason, rejected alternatives).
5. Reply with a one-paragraph summary and remind the user the session
   can now be safely cleared.

$ARGUMENTS
